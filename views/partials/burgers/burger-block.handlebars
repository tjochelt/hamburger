<li>
    {{name}}

    <button class="devour-burger" data-id="{{id}}" data-newburger="{{devoured}}">
        {{#if devoured}}Devoured!{{else}}Devour!{{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>